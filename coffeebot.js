   /* milk=lst[lst.length-1]
    let flag=1
    let cntmilk=0
    let cntsyr=0
    while (flag===1) {
        document.getElementById('payment').addEventListener("click", function () {
          flag=0
          console.log(flag)
        });
        syrop=document.getElementById('syrop');
        milk.onclick = function addMilk () {
        let cntmilk=order.filter(x => x==="Молоко").length
        order.push(milk)
        cntmilk++
        }
        syrop.onclick = function addSyrop () {
        let cntsyr =order.filter(x => x==="Сироп").length
        if (cntsyr ===100){
            alert("Нельзя брать больше 100 сиропов!")}
        else {
            order.push(syrop)
            cntsyr++
        }
    }
         
    }
    console.log(order)
}
*/